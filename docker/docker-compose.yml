
services:
  localstack:
    image: localstack/localstack:3.8.1
    ports:
      - "4566:4566"
    environment: 
      -  SERVICES=dynamodb,sqs
      -  DEFAULT_REGION=us-east-1


    container_name: care-circle-localstack